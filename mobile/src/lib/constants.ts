export const API_VERSION_PATH = '/api/v1';

export const CLIENT_PLATFORM_HEADER = 'X-Client-Platform';
export const CLIENT_PLATFORM_VALUE = 'mobile';

export const SECURE_STORAGE_KEYS = {
  accessToken: 'memora.accessToken',
  refreshToken: 'memora.refreshToken',
} as const;

export const THEME_PREFERENCE_KEY = 'memora.themePreference';
